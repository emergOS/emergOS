{
  "name": "emergos-landing-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Temporary Cloudflare Worker landing page for emergos.org.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "wrangler": "^4.0.0"
  }
}
